package me.silviogames.geha;

public enum MinerAnim
{
   IDLE,
   MOVE,
   SIMPLE_ATTACK,

   // // TODO: 26.03.23 CONTINUE HERE:
   // bring the ANIMS to life.
   // tie them logically to the global Anims, not sure right now if they are always clear
   // because when rendering Miners I need to check their current MinerAnim and from that get the
   // correct global Anim.
   // but for that I first need to load the animations actually as Anims
   // since all animations need a time, that one could be fixed state for all
   ;
}
